version: '3.8'

services:
  virolaweb:
    build: .
    container_name: virolaweb
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
